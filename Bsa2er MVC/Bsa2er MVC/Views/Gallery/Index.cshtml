@model IEnumerable<Bsa2er_MVC.Models.Gallery>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Gallery_path)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Gallery_path)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.G_Id }) 
            @Html.ActionLink("Details", "Details", new { id=item.G_Id }) 
            @Html.ActionLink("Delete", "Delete", new { id=item.G_Id })
        </td>
    </tr>
}

</table>

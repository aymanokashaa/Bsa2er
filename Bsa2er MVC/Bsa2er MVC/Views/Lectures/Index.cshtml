@model IEnumerable<Bsa2er_MVC.Models.Lecture>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Program.Program_Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lecture_Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lecture_VideoLink)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Program.Program_Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Lecture_Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Lecture_VideoLink)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Lecture_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Lecture_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Lecture_Id })
        </td>
    </tr>
}

</table>
